#include <gtest/gtest.h>
#include "List.h"

TEST(ListTests, empty_list_returns_size_0)
{
List testing;
EXPECT_EQ(0, testing.size());
}

TEST(ListTests, empty_list_returns_size_0)
{
    List testing;
    EXPECT_EQ(0, testing.size());
}